import { MusicGeneratorSettings } from './music-generator-settings';

describe('MusicGeneratorSettings', () => {
  it('should create an instance', () => {
    expect(new MusicGeneratorSettings()).toBeTruthy();
  });
});
