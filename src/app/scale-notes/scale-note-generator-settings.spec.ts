import { ScaleNoteGeneratorSettings } from './scale-note-generator-settings';

describe('ScaleNoteGeneratorSettings', () => {
  it('should create an instance', () => {
    expect(new ScaleNoteGeneratorSettings()).toBeTruthy();
  });
});
